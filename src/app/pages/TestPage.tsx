import RatingCandidateModal from "../components/ui-enterprise/modals/RatingCandidateModal";
const App: React.FC = () => {
  return <RatingCandidateModal money={1000} open={true} setOpen={() => {}} />;
};

export default App;
